# Copy to web.env on the server and fill in.
# PROXY_API_KEY must match what cse-platform expects (see cse-platform/.env).
# Used by nginx.conf.template (via envsubst) to authenticate the internal
# /api/vms-requests admin proxy to cse-platform.
PROXY_API_KEY=
